node_modules/
dist/
.vite/
.cache/
.DS_Store
.env
.env.local
.env.*.local

# Point-in-time Supabase data snapshot used only by scripts/prerender.mjs
# on a machine that can't reach Supabase directly - see that file's own
# comments. Never a source of truth, always regenerated before use.
scripts/_catalog-snapshot.json
