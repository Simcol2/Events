# Local dev placeholders — real secrets are delivered via /run/base44/app.env
# and always override these (last env_file wins). The app boots fine without
# Supabase credentials; only the Decor catalog page needs them.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
